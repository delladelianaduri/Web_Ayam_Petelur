name: ayam_petelur_web
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0

  # ✅ Firebase packages diperbarui agar sesuai dengan Dart 3.7.2
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.9
  provider: ^6.0.5 # untuk state management (opsional tapi direkomendasikan)
  syncfusion_flutter_charts: ^29.2.11
  intl: ^0.20.2

  logger: ^2.5.0 # Changed from logging to logger

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8

  # Tambahkan package untuk QR scanner
  qr_code_scanner: ^1.0.1

  google_fonts: ^6.1.0  # Ditambahkan untuk font Poppins

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
        - asset: assets/fonts/Poppins-SemiBold.ttf
          weight: 600
